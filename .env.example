# Copy to .env for local overrides. All values are optional; real config lives in
# %APPDATA%\AIWorkstationManager\config.toml (created on first run).

# AIWM_DATA_DIR=E:\AI\.localdata   # override the config/db/logs root (portable install)
# AIWM_LOG=info,aiwm_core=debug
# AIWM_CORE_API_PORT=48160
# AIWM_STORE_PATH=E:\AI\models
# AIWM_OFFLINE=false
