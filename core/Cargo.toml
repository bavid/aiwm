[package]
name = "aiwm-core"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "AI Workstation Manager — core logic (orchestration, scheduling, model registry, telemetry)."
publish = false

[lints]
workspace = true

[dependencies]
tokio.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
anyhow.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true
tracing-appender.workspace = true
toml.workspace = true
uuid.workspace = true
time.workspace = true
# Added in later WP-0..WP-8 steps as each module is implemented:
#   sqlx (WP-2), nvml-wrapper + sysinfo (WP-3), win32job (WP-4),
#   axum + tower (WP-6), reqwest (WP-2+, dormant until Phase 2)

[dev-dependencies]
tokio = { workspace = true, features = ["test-util"] }
