//! Persistence: sqlx SQLite pool, migration runner (`core/migrations`), and
//! repository traits (`ModelRepo`, `JobRepo`, `RuntimeRepo`, `SettingsRepo`)
//! with SQLite implementations. Implemented in WP-2. Schema v1 DDL is in
//! `docs/PHASE_1_PLAN.md` §4.
